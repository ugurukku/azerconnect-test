spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/azerconnect_test
    username: postgres
    password: ugur2003
  application:
    name: USER-SERVICE
  config:
    import: optional:configserver:http://localhost:8085